import React from "react";

import LogoutButton from "../LogoutButton/index";
import Profile from "../Profile/index";

function NoticeBoard() {
  return (
    <div>
      <h1>Kingsheath Noticeboard</h1>
      <LogoutButton />
      <Profile />
    </div>
  );
}

export default NoticeBoard;

// Input component

// Note components
//Props
